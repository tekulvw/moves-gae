service: transcoder
runtime: custom
env: flex

runtime_config:
  python_version: 3

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 1

env_variables:
  PROJECT_ID: moves-gae
  STORAGE_BUCKET: moves-gae.appspot.com
  TRANSCODE_TOPIC: transcode_request
  VIDEO_PREFIX: videos